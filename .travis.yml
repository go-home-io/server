sudo: false
language: go
go:
  - "1.11beta1"
install:
  - make utilities
  - go get github.com/go-home-io/providers
jobs:
  include:
    - stage: test
      script: make test
    - stage: build
      script: make build-plugins build